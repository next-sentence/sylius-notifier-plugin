{% import '@SyliusAdmin/shared/helper/button.html.twig' as button %}

{% set path = path(options.link.route, options.link.parameters|default({})) %}

{{ button.default({ text: action.label|trans, icon: 'eye', url: path, attr: sylius_test_html_attribute('reset') }) }}

